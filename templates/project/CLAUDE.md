# CLAUDE.md — <PROJECT NAME>

> Written by `studio onboard`. This file holds **what is true about this project**.
> It deliberately contains **no** MCP, plugin, or skill configuration: every capability is
> inherited from the user-scope environment and is already available here. If you are
> tempted to add a `.mcp.json` to this repository, read `docs/architecture.md` in
> ai-studio-infra first — per-project MCP config is the failure mode this system removes.

## What this is

<One paragraph: what the product is, who it serves, what "done" means.>

## Stack

- Language / framework:
- Package manager:
- Styling:
- Testing:
- Hosting / deploy:

## Commands

```
install
dev
build
test
lint
typecheck
```

## Conventions that are not obvious from the code

<Naming, folder layout, state boundaries, i18n approach, anything a competent newcomer
would otherwise get wrong. Keep it to things the code does not already say.>

## Binding facts — do not change without the owner

<Prices, legal text, domains, contact addresses, brand marks, localisation rules, SEO
constraints, production behaviour. These are facts, not preferences. Surface a concern and
propose; never quietly edit them.>

## Known constraints and sharp edges

<Things that will bite: fragile scripts, rate limits, seat/plan limits, manual steps,
environment variables that must be set outside the repo.>

## Out of scope

<What Claude should not touch here.>
